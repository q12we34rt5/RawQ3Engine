import { Var, Prefix, DType } from "../constants.js";

export default {
    codes: {
        fragment: {
            global: [
                `precision mediump float;`,
                ``,
            ],
        },
    }
};