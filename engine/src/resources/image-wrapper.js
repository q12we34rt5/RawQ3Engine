import { ResourceMonitor, ResourceWrapper } from "../core/resource-monitor";

export const imageMonitor = new ResourceMonitor();

export class ImageWrapper extends ResourceWrapper {

    constructor() { }

}
